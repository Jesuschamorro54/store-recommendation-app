# Store recommendation app

## Run Enviroment
Go to path **/app/env/scripts** an Run
```
activate
```

## Development Setup
Run the app in debug mode. Enter the project directory **/app**
```
flask --app app --debug run
```